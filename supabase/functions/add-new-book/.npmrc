# NPM configuration for Supabase Edge Functions
registry=https://registry.npmjs.org/
# Enable legacy peer deps for better compatibility
legacy-peer-deps=true
# Set timeout for npm installs
timeout=60000
